---
# Foreman deployment via Helm chart (no VMs, only Helm-deployed infrastructure)
virtualmachines: []
containers: []

# Future enhancement: Foreman CLI container with hammer
# Requires custom container image with hammer-cli pre-installed (dnf requires root)
# See research in cursor-revisit/troubleshooting/ for implementation details
#
# containers:
#   - name: foreman-tools
#     image: quay.io/YOUR_ORG/foreman-tools:3.13  # Custom image needed
#     cpu: "250m"
#     memory: "1Gi"
#     environment: {}
#     command:
#       - /bin/sh
#       - -c
#       - sleep infinity
#     args: []
#     ports:
#       - containerPort: 8080
#         protocol: TCP
#     terminals:
#       - name: foreman-cli
#         command: /bin/bash
#     commands:
#       - sed -i 's/PLACEHOLDER_PASSWORD/{{ common_password }}/' /root/.hammer/cli.modules.d/foreman.yml
