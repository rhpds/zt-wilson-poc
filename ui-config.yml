---

antora:
  name: modules
  dir: www
  modules:
    - name: module-01
      label: "Deploying Foreman with Helm"
      solveButton: false

tabs:
  - name: "AAP Web UI"
    url: http://aap.sandbox-${guid}-zt-rhelbu.svc/
  # Future: Foreman Web UI tab (placeholder from template)
  # - name: "Foreman Web UI"
  #   url: https://foreman-${guid}.${domain}/
  # Future: Foreman CLI tab (requires custom container image with hammer-cli)
  # - name: "Foreman CLI"
  #   url: /foreman-cli
